{
  "name": "demoweb",
  "version": "1.0.0",
  "description": "DemoWeb - Full-stack application with React frontend and serverless API",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm run preview"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^9.0.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
